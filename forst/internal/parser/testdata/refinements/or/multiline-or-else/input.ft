package main

type Error = String

func f(x String, value String, status ActiveStatus): Error {
    ensure value is String.Min(3).Max(32)
    or UUID.V4()
    or Slug.Valid()
    else InvalidValue{}
    return ""
}

type ActiveStatus = String
type InvalidStatus = String
type A = String
type B = String
type X = { kind: String }
