<Tabs>
  <Tab title="Forst">
    ```ft
    type Named = {
    	name: String,
    }

    type Entity = {
    	id: Int,
    }

    type NamedEntity = Named & Entity
    ```
  </Tab>
  <Tab title="Generated Go">
    ```go
    // forst build — not emitted yet
    // typecheck: intersection Named & Entity is empty (unrelated types)
    // Complete narrowing and Go types for intersections remain in development.
    ```
  </Tab>
</Tabs>
