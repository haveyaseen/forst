<Tabs>
  <Tab title="Forst">
    ```ft
    order := placeOrder({
    	stockKeepingUnit: "ITEM-1",
    	quantity:         2,
    })
    ensure order
    println(order)
    ```
  </Tab>
  <Tab title="Generated Go">
    ```go
    // forst build
    order, err := placeOrder(PlaceOrderInput{
    	stockKeepingUnit: "ITEM-1",
    	quantity:         2,
    })
    if err != nil {
    	return err
    }
    println(order)
    ```
  </Tab>
</Tabs>
